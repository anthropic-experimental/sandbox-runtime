node_modules
dist
.claude
.DS_Store
# Built launcher binaries (regenerated by `npm run build:launcher`; the
# release workflow builds and bundles them into the published package).
vendor/srt-launcher/
vendor/srt-launcher-rs/target/
